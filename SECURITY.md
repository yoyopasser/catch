# 資料安全說明

- 不要提交 `data/stock_tool.db`、匯出檔、行情快取、`.env` 或 API Token。
- Codespaces 的 `8501` 連接埠請維持 **Private**；目前工具沒有登入系統。
- Codespace 被刪除時本機資料也會消失，請定期使用網站內的「下載完整備份」。
- FinMind Token 不會被 Git 提交，完整備份也刻意排除 Token。
